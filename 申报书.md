### 1. 项目名称
MoonDNS —— 100% 纯 MoonBit（Zero-FFI）高性能权威域名服务器与协议安全防御引擎

### 2. 项目简介
MoonDNS 是基于 MoonBit 从零独立研发的原生系统级权威域名解析引擎（Authoritative DNS Server），严格遵循 IETF RFC 1034、RFC 1035 与 RFC 6891 (EDNS0) 国际标准。针对传统 DNS 服务器（如 BIND9、CoreDNS）体积庞大、依赖繁重及易受压缩指针循环攻击的痛点，MoonDNS 创新实现了形式化四重防死循环指针解压状态机、协议与网络 I/O 深度解耦的纯函数状态机，以及针对 WebAssembly 微沙箱环境的原生编译与运行支持。项目填补了 MoonBit 生态在 RFC 1035/6891 权威解析核心与协议层报文安全防线上的空白，为云原生 Serverless、边缘计算网络及轻量级私有云提供了高安全性、确定性与微秒级冷启动的纯 MoonBit 协议基础设施。

### 3. 项目方向、生态定位与通用性说明
- **项目方向**：系统与网络基础设施 / 网络协议栈与安全中间件 / 云原生与 WebAssembly 运行时生态。
- **生态定位客观说明**：
  MoonBit 社区在通用网络与运行时探索上已有成果，但在核心底层网络协议栈（特别是规范严密、安全要求极高的权威域名解析与报文防线）领域，此前社区开源工具仅局限于客户端简易解析存根（`moon-dns-stub`）与离线 Zone 语法审计工具（`moonbit-dns-zone`）。MoonDNS 并非宽泛的网络服务器，而是**精准聚焦于微内核、零 FFI、高确定性的纯 MoonBit 权威 DNS 解析引擎与防御中间件**，补齐了协议层服务端的核心基石。
- **通用性说明**：
  1. **架构契约彻底解耦**：核心协议编解码器、内存区域数据库树与权威应答构造机均为 100% 纯 MoonBit 编写的确定性纯函数（`Bytes -> Response Bytes`），不依赖操作系统原生 Socket 或 C 语言绑定（100% Zero-FFI），具备极高的宿主环境可移植性。
  2. **双模态编译与极速运行**：既可作为独立服务二进制部署于 Linux/Windows/macOS 服务器和工控边缘网关，更可直接编译为轻量化 WebAssembly（WASI Preview 1，产物体积仅 160 KB），内嵌于 Cloudflare Workers、Fastly Compute 等边缘网络及无物理 Socket 沙箱中运行。
  3. **生态可复用构件**：核心编解码包与安全解压器可独立发布为 Mooncakes 通用包（`chgttyyr/moon_dns`），供整个 MoonBit 社区开发 DNS 代理、安全网关、服务发现等后续工具直接复用。

### 4. 预期使用场景
1. **WebAssembly 边缘网络无服务器（Serverless）权威 DNS 节点**：在 Cloudflare Workers、Fastly 等无物理网卡的边缘微沙箱中，利用 MoonDNS 纯函数字节流接口处理 DNS 流量，实现低延迟私有域名解析与安全路由。
2. **轻量级私有云与局域网权威域名服务器**：企业内部私有域名、局域网内网集群及容器云私有域名的高速解析与权威托管，内存占用小于 5 MB，启动毫秒级，彻底消除传统 BIND9 臃肿配置与 C 语言内存越界隐患。
3. **安全关键与嵌入式设备防御型域名基础设施**：在工业边缘网关、路由器或物联网终端中，利用 MoonDNS 内建的四重恶意指针防御体系，彻底免疫因经典压缩指针自环、交叉环和越界跳转引发的崩溃与拒绝服务攻击（DoS）。

### 5. 核心功能与技术差异性壁垒
MoonDNS 不做通用功能的简单堆砌，针对业界传统 DNS 实现（如 BIND9、CoreDNS）的痛点，重点构建了四大核心技术壁垒：

1. **差异化壁垒一：形式化四重防死循环压缩指针防御状态机（确定性防御）**
   - **痛点与突破**：针对 DNS 历史上最致命的压缩指针炸弹攻击（如 CVE-2000-0333、CVE-2020-8616 算法放大 DoS），传统实现依赖脆弱的递归深度检测。MoonDNS 构建了“越界拦截 + 自指/前向递增校验 + 访问位图环路熔断 + 128 跳硬截断”四重形式化防御状态机，在类型系统与算法层面保证面对任意畸形攻击报文**绝对零 Panic、零死循环、零内存泄漏**。
2. **差异化壁垒二：纯函数协议状态机与零 I/O 深度解耦（100% Zero-FFI）**
   - **痛点与突破**：传统 DNS（如 CoreDNS、BIND9）强绑定宿主 epoll/netpoll 网络线程池，无法脱离物理 Socket 运行。MoonDNS 将全流程抽象为无状态纯函数（`QueryBytes -> Message -> ZoneLookup -> ResponseBytes`），绝无任何外部 C/JS FFI，使协议引擎天然跨越 Native、内存虚拟管道（MockChannel）与 Wasm 宿主函数。
3. **差异化壁垒三：自适应出站压缩后缀字典与零拷贝标签游标**
   - **优化与指标**：基于游标实现变长 Label 二进制切片安全解析，避免大量中间字符串堆分配；在报文出站序列化时，动态自适应构建后缀匹配字典树，最大化域名压缩率，有效降低 UDP 512 字节截断概率并显著节省边缘网络开销。
4. **差异化壁垒四：面向边缘微沙箱的超轻量与微秒级冷启动**
   - **对比优势**：相比 CoreDNS 庞大的数十 MB 容器镜像与 Go GC 暂停抖动，MoonDNS 编译产物体积仅 160.72 KB，启动微秒级，内存开销数兆字节，无 GC 抖动，专为边缘 Serverless 容器与微沙箱定制。
5. **完整严格的 RFC 1035/6891 协议规范支持**：
   - 覆盖 A、AAAA、CNAME、NS、MX、TXT（多分块字符串）、SOA（完整 7 元组）、PTR、SRV、CAA 及 EDNS0 OPT 伪记录；
   - 内置标准 BIND Master-file 文本语法解析器，支持 `$ORIGIN`、`$TTL` 与 `@` 宏；
   - 严格实现权威语义：AA=1 准确置位、Exact/Wildcard 树形检索、CNAME 链式追踪、NODATA/NXDOMAIN 携带 SOA 负向缓存及 UDP 512 / EDNS0 报文安全截断（TC=1）。

### 6. WebAssembly 运行验证事实依据
针对初审对 Wasm 运行依据的关切，项目提供了完整、可复现的多层级验证实证：
1. **多目标全量自动化测试实证**：
   - 在 MoonBit 官方工具链下执行自动化测试：
     - `moon test --target wasm`：全量 57 项测试 **100% 通过（PASSED 57/57）**；
     - `moon test --target wasm-gc`：全量 57 项测试 **100% 通过（PASSED 57/57）**；
     - `moon test --target js`：全量 57 项测试 **100% 通过（PASSED 57/57）**。
2. **标准 WASI 字节码编译产物验证**：
   - 执行 `moon build --target wasm`，生成标准 `wasm32-unknown-wasi` 产物 `_build/wasm/debug/build/cmd/main/main.wasm`；
   - 产物体积仅 **160.72 KB**（164,579 bytes），系统导入表仅包含单个 `wasi_snapshot_preview1.fd_write`，证明 100% Zero-FFI 无外部宿主专有绑定。
3. **独立 Node.js WASI 宿主环境端到端执行实证**：
   - 仓库内建独立验证脚本 `scripts/verify_wasm.js` 与 `scripts/verify_wasm.ps1`，脱离 MoonBit 编译器，直接在标准 Node.js WASI Preview 1 运行时中实例化执行；
   - 真实验证了在 Wasm 沙箱内部完成 Zone 加载、A 记录权威解析、CNAME 链式追踪、通配符解析、NODATA/NXDOMAIN 生成以及恶意指针死循环攻击的主动拦截（Exit Code = 0，零 Panic）。

### 7. 项目属性与原创性声明
- **项目属性**：原创项目（由开发者 `chgttyyr` 基于 MoonBit 从零独立架构与实现）。
- **参考与来源说明**：无外部专有代码移植与复制。系统严格遵循国际互联网工程任务组 IETF 官方发布的标准规范（RFC 1034、RFC 1035、RFC 6891、RFC 3597）独立手写实现。调研阶段查阅了社区现有客户端存根（`jinshengmeng46/moon-dns-stub`）与配置检查工具（`lmclmc1/moonbit-dns-zone`）的接口边界，确立了补齐权威服务端核心的定位，无代码复用。

### 8. GitHub 仓库链接与提交说明
- **仓库链接**：https://github.com/chgttyyr/moon_dns
- **提交情况**：全库由开发者 `chgttyyr` 独立开发，全链历史 Author 100% 唯一（`chgttyyr <299825988+chgttyyr@users.noreply.github.com>`）。目前已完成 21+ 个真实有效、符合 Conventional Commits 规范的语义化递进 commits（涵盖骨架构建、协议类型、大端序字节IO、变长标签编解码、全记录类型、四重指针安全防线、内存Zone数据库、权威应答机、截断机制、双模传输、CLI工具集、Wasm验证套件及 57 项自动化测试的完整工程演进）。纯 MoonBit 核心源码 4,586 行，工程总规模 5,500+ 行，多目标全量测试 100% 绿灯通过，无任何虚假拆分或空提交。
